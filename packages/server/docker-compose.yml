services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: stello
      POSTGRES_PASSWORD: stello
      POSTGRES_DB: stello_test
    ports:
      - "5432:5432"
    tmpfs:
      - /var/lib/postgresql/data
